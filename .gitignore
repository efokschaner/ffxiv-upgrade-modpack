# Local research copies of the C# sources we are porting from (not ours to commit).
# Re-clone from the URLs in docs/superpowers/specs if missing:
#   https://github.com/TexTools/xivModdingFramework  (branch: develop)
#   https://github.com/TexTools/FFXIV_TexTools_UI
/reference/

# Node
node_modules/
dist/
coverage/
*.log

# OS
Thumbs.db
.DS_Store

# Subagent-driven-development scratch
/.superpowers/

# Local-only notes / scratch findings (not committed; e.g. per-machine modpack scans)
/local-notes/

# Modpack corpus (test/corpus/real/ real mods + test/corpus/synthetic/ authored packs) + goldens
# and caches for converter tests — local-only, not committed.
/test/corpus/
